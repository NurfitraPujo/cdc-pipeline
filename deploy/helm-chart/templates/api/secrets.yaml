{{- if .Values.api.env.secrets }}
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ .Values.api.name }}
spec:
  encryptedData:
{{ toYaml .Values.api.env.secrets | indent 4 }}
{{- end }}

