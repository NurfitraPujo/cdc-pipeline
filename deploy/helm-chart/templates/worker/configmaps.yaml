apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.worker.name }}
  labels:
    app.kubernetes.io/name: {{ .Values.worker.name }}
    app.kubernetes.io/environment: {{ .Values.environment }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
{{- with .Values.worker.env.configmap }}
data:
{{ toYaml . | indent 2 }}
{{- end }}
